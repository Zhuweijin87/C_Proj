#include "list.h"

static List *link_node_create(void *data)
{
	struct link_node *node = malloc(sizeof(struct link_node));
}

List *List_Create(DataType type, int size)
{
	List *list;
}

int List_GetSize(List *list)
{
	int lsize = 0 ;

	return lsize;
}

int List_Add(List **list, void *data)
{
	List *pList = *list;
	if(plist == NULL) return -1;
	
	
}

int List_Del(List **list, int pos)
{

}
